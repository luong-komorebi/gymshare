%h4 Add new round
= simple_form_for [@workout_plans, @round] do |f|
  = f.input :repitition, as: :integer, label: false, input_html: { min: 0 }
  = render partial: 'exercises/form', locals: { containing_round: f }
  = f.button :submit
  = link_to 'Cancel', workout_plans_path

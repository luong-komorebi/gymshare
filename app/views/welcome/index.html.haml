%h1 Hello World

- if current_user
  ="Signed in as #{current_user.name}"
  = link_to "Logout", '/logout'
  = link_to "Explore", '/workout_plans/'
  = link_to "Create new", '/workout_plans/new'
- else
  = link_to 'Login', '/login'
  = link_to 'Signup', '/signup'